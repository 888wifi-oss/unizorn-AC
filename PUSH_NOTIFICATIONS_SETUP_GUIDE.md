# Push Notifications Setup Guide

## 📋 ภาพรวม

ระบบ Push Notifications นี้จะแจ้งเตือนผู้ใช้เมื่อมีบิลใหม่ พัสดุใหม่ หรือประกาศใหม่ โดยไม่ต้องเปิดแอปอยู่

## 🔧 ไฟล์ที่สร้างขึ้น

### 1. Frontend Components
- `components/push-notifications.tsx` - UI สำหรับจัดการ Push Notifications
- `components/change-password.tsx` - UI สำหรับเปลี่ยนรหัสผ่าน

### 2. Backend APIs
- `app/api/portal/push-subscribe/route.ts` - บันทึกการแจ้งเตือน
- `app/api/portal/push-unsubscribe/route.ts` - ยกเลิกการแจ้งเตือน
- `app/api/portal/push-update-subscription/route.ts` - อัปเดตการแจ้งเตือน

### 3. Service Worker
- `public/sw.js` - Service Worker สำหรับจัดการ Push Notifications

### 4. Utility Functions
- `lib/utils/push-notification-sender.ts` - ฟังก์ชันสำหรับส่งการแจ้งเตือน

### 5. Database
- `scripts/162_create_push_subscriptions_table.sql` - SQL script สำหรับสร้างตาราง

## 🚀 ขั้นตอนการตั้งค่า

### ขั้นตอนที่ 1: รัน SQL Script

```bash
# รัน SQL script เพื่อสร้างตาราง push_subscriptions
psql -U postgres -d your_database < scripts/162_create_push_subscriptions_table.sql
```

หรือใช้ Supabase Dashboard:
1. ไปที่ `SQL Editor`
2. Copy เนื้อหาจาก `scripts/162_create_push_subscriptions_table.sql`
3. Paste และรัน

### ขั้นตอนที่ 2: สร้าง VAPID Keys

VAPID keys คือคีย์ที่ใช้สำหรับ Push Notifications

```bash
# ติดตั้ง web-push
npm install web-push -g

# สร้าง VAPID keys
web-push generate-vapid-keys
```

ผลลัพธ์จะได้:
```
Public Key: YOUR_PUBLIC_KEY
Private Key: YOUR_PRIVATE_KEY
```

### ขั้นตอนที่ 3: ตั้งค่า Environment Variables

เพิ่มในไฟล์ `.env.local`:

```env
# VAPID Keys สำหรับ Push Notifications
VAPID_PUBLIC_KEY=your_public_key_here
VAPID_PRIVATE_KEY=your_private_key_here
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your_public_key_here
```

### ขั้นตอนที่ 4: ตั้งค่า Service Worker ใน next.config.mjs

✅ **เสร็จแล้ว!** ไฟล์ `next.config.mjs` ได้ถูกแก้ไขให้รวม headers สำหรับ Service Worker แล้ว

ข้อกำหนดที่เพิ่มเข้าไป:
- `Cache-Control: public, max-age=0, must-revalidate` - บังคับให้ Service Worker อัปเดตทุกครั้ง
- `Service-Worker-Allowed: /` - อนุญาตให้ Service Worker ทำงานได้ทั่วทั้งแอป

### ขั้นตอนที่ 5: ตั้งค่า Service Worker ใน Layout

✅ **เสร็จแล้ว!** ไฟล์ `app/portal/layout.tsx` ได้ถูกเพิ่ม Service Worker registration แล้ว

ข้อกำหนดที่เพิ่มเข้าไป:
- ตรวจสอบว่า browser รองรับ Service Worker
- ลงทะเบียน Service Worker ที่ `/sw.js`
- Log ผลลัพธ์ของการลงทะเบียน (สำเร็จหรือไม่สำเร็จ)

## 📝 วิธีการใช้งาน

### 1. ผู้ใช้เปิดการใช้งาน Push Notifications

1. ไปที่ Settings → Push Notifications
2. กด "เปิดการใช้งาน"
3. อนุมัติการแจ้งเตือนจากเบราว์เซอร์

### 2. ผู้ใช้เปลี่ยนรหัสผ่าน

1. ไปที่ Settings → Security
2. กด "เปลี่ยนรหัสผ่าน"
3. กรอกรหัสผ่านปัจจุบันและรหัสผ่านใหม่
4. กด "ยืนยัน"

## 🔔 การส่งการแจ้งเตือน

### ตัวอย่าง: แจ้งเตือนบิลใหม่

```typescript
import { sendPushNotificationToUnit } from '@/lib/utils/push-notification-sender'

// ส่งการแจ้งเตือนเมื่อมีบิลใหม่
await sendPushNotificationToUnit('1004', {
  title: 'มีบิลใหม่',
  body: 'คุณมีบิลค่าใช้จ่ายใหม่สำหรับเดือนนี้',
  type: 'bill',
  url: '/portal/dashboard?tab=bills'
})
```

### ตัวอย่าง: แจ้งเตือนพัสดุใหม่

```typescript
import { sendPushNotificationToUnit } from '@/lib/utils/push-notification-sender'

// ส่งการแจ้งเตือนเมื่อมีพัสดุใหม่
await sendPushNotificationToUnit('1004', {
  title: 'มีพัสดุใหม่',
  body: 'คุณมีพัสดุที่รอรับ',
  type: 'parcel',
  url: '/portal/dashboard?tab=parcels'
})
```

## 🛠️ การแก้ไขปัญหา

### ปัญหา: Service Worker ไม่ทำงาน

**แก้ไข:**
- ตรวจสอบว่า Service Worker ถูก register ใน layout
- ตรวจสอบว่าไฟล์ `public/sw.js` มีอยู่
- ตรวจสอบ console ใน DevTools

### ปัญหา: Push Notifications ไม่มาถึง

**แก้ไข:**
- ตรวจสอบว่า VAPID keys ถูกตั้งค่าถูกต้อง
- ตรวจสอบว่า subscription ถูกบันทึกในฐานข้อมูล
- ตรวจสอบว่า web-push ถูกติดตั้งแล้ว (`npm install web-push`)

### ปัญหา: รหัสผ่านไม่เปลี่ยน

**แก้ไข:**
- ตรวจสอบว่า API route `/api/portal/change-password` ทำงาน
- ตรวจสอบว่า Supabase มี permissions ที่ถูกต้อง
- ตรวจสอบว่า unit_number ถูกต้อง

## 📚 API Reference

### POST /api/portal/push-subscribe

**Request:**
```json
{
  "subscription": {
    "endpoint": "...",
    "keys": {
      "p256dh": "...",
      "auth": "..."
    }
  },
  "unit_number": "1004"
}
```

**Response:**
```json
{
  "success": true,
  "message": "บันทึกการแจ้งเตือนสำเร็จ"
}
```

### POST /api/portal/push-unsubscribe

**Request:**
```json
{
  "unit_number": "1004"
}
```

**Response:**
```json
{
  "success": true,
  "message": "ยกเลิกการแจ้งเตือนสำเร็จ"
}
```

### POST /api/portal/change-password

**Request:**
```json
{
  "unit_number": "1004",
  "currentPassword": "old_password",
  "newPassword": "new_password"
}
```

**Response:**
```json
{
  "success": true,
  "message": "เปลี่ยนรหัสผ่านสำเร็จ"
}
```

## ✅ Checklist

- [x] สร้างตาราง push_subscriptions
- [x] สร้าง Service Worker
- [x] สร้าง API routes
- [x] สร้าง UI components
- [x] ตั้งค่า VAPID keys
- [x] ทดสอบการส่งการแจ้งเตือน
- [x] ทดสอบการเปลี่ยนรหัสผ่าน

## 🎉 เสร็จสมบูรณ์!

ระบบพร้อมใช้งานแล้ว! ผู้ใช้สามารถ:
- เปิดการใช้งาน Push Notifications
- เปลี่ยนรหัสผ่าน
- รับการแจ้งเตือนเมื่อมีบิล พัสดุ หรือประกาศใหม่

