# 🔧 แก้ไขปัญหา Login และสถานะบัญชี

## ปัญหาที่พบ

### 1. Login ด้วย Username ไม่ได้
**ปัญหา:** ต้องใช้ email เต็มในการ login
**สาเหตุ:** Supabase Auth ไม่รองรับ username โดยตรง

### 2. สถานะบัญชีไม่เปลี่ยน
**ปัญหา:** หลังลงทะเบียนสำเร็จ สถานะยังเป็น "pending"
**สาเหตุ:** การอัปเดตสถานะ invitation อาจมีปัญหา

## วิธีแก้ไข

### สำหรับปัญหา 1: Login ด้วย Username

**วิธีที่ 1: ใช้ Email เต็ม (แนะนำ)**
```
Username: ADD2@unizorn.local
Password: 12345678
```

**วิธีที่ 2: ตรวจสอบ Username ที่ถูกต้อง**
1. ไปที่ Supabase Dashboard → Authentication → Users
2. หา email ที่เกี่ยวข้อง
3. ดูใน User Metadata → username
4. ใช้ username ที่ถูกต้อง

### สำหรับปัญหา 2: สถานะบัญชี

**ตรวจสอบใน Supabase:**
1. ไปที่ Table Editor → invitations
2. หา invitation ที่ใช้
3. ตรวจสอบ status และ used_at

**ถ้าสถานะยังเป็น "pending":**
```sql
UPDATE invitations 
SET status = 'used', used_at = NOW() 
WHERE code = 'your-invitation-code';
```

## การทำงานของระบบ

### Email Format ที่ใช้:
```
Username: ADD2
→ Email: ADD2@unizorn.local
```

### Database Updates:
```sql
-- เมื่อสร้างบัญชีสำเร็จ
UPDATE units 
SET user_id = 'auth-user-id', owner_email = 'ADD2@unizorn.local'
WHERE unit_number = 'ADD2';

UPDATE invitations 
SET status = 'used', used_at = NOW()
WHERE code = 'invitation-code';
```

## Troubleshooting

### ถ้า Login ไม่ได้:
1. **ใช้ Email เต็ม:** `ADD2@unizorn.local`
2. **ตรวจสอบ Password:** ต้องตรงกับที่ตั้งไว้
3. **ตรวจสอบใน Supabase:** ว่ามี user อยู่จริง

### ถ้าสถานะไม่เปลี่ยน:
1. **ตรวจสอบ Console Log:** ดู error message
2. **ตรวจสอบ Database:** ดูตาราง invitations
3. **อัปเดตด้วย SQL:** ถ้าจำเป็น

## ตัวอย่างการใช้งาน

### Login ที่ถูกต้อง:
```
Username: ADD2@unizorn.local
Password: 12345678
```

### ตรวจสอบสถานะ:
```sql
SELECT * FROM invitations 
WHERE unit_id = 'unit-id' 
ORDER BY created_at DESC;
```

## สรุป

**ปัญหาหลัก:** ต้องใช้ email เต็มในการ login
**วิธีแก้:** ใช้ `ADD2@unizorn.local` แทน `ADD2`

**สถานะบัญชี:** ตรวจสอบใน database และอัปเดตด้วย SQL ถ้าจำเป็น
