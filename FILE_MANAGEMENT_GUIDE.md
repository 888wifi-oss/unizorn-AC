# File Management System - ระบบจัดการเอกสารและไฟล์

## ภาพรวม
ระบบจัดการเอกสารและไฟล์ที่ครอบคลุมสำหรับคอนโดมิเนียม ประกอบด้วยการอัปโหลด จัดหมวดหมู่ ค้นหา และจัดการสิทธิ์การเข้าถึงไฟล์

## ฟีเจอร์หลัก

### 📁 **การจัดการไฟล์**
- ✅ อัปโหลดไฟล์หลายประเภท (เอกสาร, รูปภาพ, วิดีโอ, ไฟล์เสียง)
- ✅ จัดหมวดหมู่ไฟล์ตามประเภท
- ✅ ค้นหาและกรองไฟล์
- ✅ ดาวน์โหลดไฟล์
- ✅ ลบไฟล์
- ✅ แก้ไขข้อมูลไฟล์

### 🏷️ **การจัดหมวดหมู่**
- ✅ สร้างหมวดหมู่ใหม่
- ✅ จัดกลุ่มไฟล์ตามหมวดหมู่
- ✅ หมวดหมู่เริ่มต้น:
  - เอกสารทั่วไป
  - สัญญา
  - บิลและใบเสร็จ
  - รายงาน
  - ภาพถ่าย
  - วิดีโอ
  - เอกสารการซ่อมบำรุง
  - เอกสารพัสดุ
  - เอกสารการเงิน
  - เอกสารอื่นๆ

### 🔐 **การจัดการสิทธิ์**
- ✅ กำหนดสิทธิ์การเข้าถึงไฟล์
- ✅ แบ่งแยกไฟล์สาธารณะและส่วนตัว
- ✅ กำหนดสิทธิ์ตามห้องชุด
- ✅ ติดตามการดาวน์โหลด

### 📊 **สถิติและรายงาน**
- ✅ สถิติไฟล์ทั้งหมด
- ✅ ขนาดไฟล์รวม
- ✅ ไฟล์ตามหมวดหมู่
- ✅ ไฟล์ตามประเภท
- ✅ ไฟล์ที่อัปโหลดล่าสุด
- ✅ ไฟล์ที่ดาวน์โหลดมากที่สุด

## ไฟล์ที่สร้างใหม่

### **Database Schema**
- `scripts/010_create_file_management.sql` - สร้างตารางสำหรับระบบจัดการไฟล์

### **TypeScript Types**
- `lib/types/file-management.ts` - ประเภทข้อมูลสำหรับระบบจัดการไฟล์

### **Server Actions**
- `lib/supabase/file-management-actions.ts` - ฟังก์ชันสำหรับจัดการไฟล์

### **UI Components**
- `app/(admin)/file-management/page.tsx` - หน้าจัดการไฟล์สำหรับ Admin

## วิธีใช้งาน

### **สำหรับ Admin**

#### **1. เข้าถึงระบบ**
1. ไปที่เมนู **"จัดการเอกสารและไฟล์"** ใน Sidebar
2. ระบบจะแสดงหน้า File Management

#### **2. อัปโหลดไฟล์**
1. กดปุ่ม **"อัปโหลดไฟล์"**
2. เลือกไฟล์ที่ต้องการอัปโหลด
3. เลือกหมวดหมู่
4. เพิ่มคำอธิบาย (ถ้าต้องการ)
5. กำหนดสิทธิ์การเข้าถึง
6. กดปุ่ม **"อัปโหลด"**

#### **3. จัดการหมวดหมู่**
1. ไปที่แท็บ **"หมวดหมู่"**
2. กดปุ่ม **"เพิ่มหมวดหมู่"**
3. กรอกชื่อหมวดหมู่
4. เพิ่มคำอธิบาย
5. เลือกสีและไอคอน
6. กดปุ่ม **"สร้างหมวดหมู่"**

#### **4. ค้นหาไฟล์**
1. ใช้ช่องค้นหาเพื่อค้นหาตามชื่อผู้อัปโหลด
2. เลือกหมวดหมู่เพื่อกรองไฟล์
3. กดปุ่ม **"ค้นหา"**

#### **5. จัดการไฟล์**
- **ดูไฟล์**: กดปุ่ม 👁️ (Eye)
- **ดาวน์โหลด**: กดปุ่ม ⬇️ (Download)
- **แก้ไข**: กดปุ่ม ✏️ (Edit)
- **ลบ**: กดปุ่ม 🗑️ (Trash)

#### **6. ดูสถิติ**
1. ไปที่แท็บ **"สถิติ"**
2. ดูข้อมูลสถิติต่างๆ:
   - ไฟล์ทั้งหมด
   - ขนาดไฟล์รวม
   - จำนวนหมวดหมู่
   - ไฟล์ตามหมวดหมู่
   - ไฟล์ตามประเภท

## Database Tables

### **file_categories**
- `id` - รหัสหมวดหมู่
- `name` - ชื่อหมวดหมู่
- `description` - คำอธิบาย
- `icon` - ไอคอน
- `color` - สี
- `is_active` - สถานะการใช้งาน

### **files**
- `id` - รหัสไฟล์
- `filename` - ชื่อไฟล์ในระบบ
- `original_filename` - ชื่อไฟล์เดิม
- `file_path` - เส้นทางไฟล์
- `file_size` - ขนาดไฟล์
- `mime_type` - ประเภทไฟล์
- `category_id` - รหัสหมวดหมู่
- `uploaded_by` - ผู้อัปโหลด
- `unit_number` - เลขห้องชุด
- `description` - คำอธิบาย
- `tags` - แท็ก
- `is_public` - สาธารณะหรือไม่
- `is_active` - สถานะการใช้งาน
- `version` - เวอร์ชัน
- `download_count` - จำนวนการดาวน์โหลด

### **file_permissions**
- `id` - รหัสสิทธิ์
- `file_id` - รหัสไฟล์
- `unit_number` - เลขห้องชุด
- `permission_type` - ประเภทสิทธิ์ (read/write/admin)
- `granted_by` - ผู้ให้สิทธิ์
- `expires_at` - วันหมดอายุ

### **file_downloads**
- `id` - รหัสการดาวน์โหลด
- `file_id` - รหัสไฟล์
- `downloaded_by` - ผู้ดาวน์โหลด
- `unit_number` - เลขห้องชุด
- `download_ip` - IP Address
- `user_agent` - User Agent
- `downloaded_at` - เวลาดาวน์โหลด

## การติดตั้ง

### **1. รัน SQL Script**
```sql
-- รันไฟล์ scripts/010_create_file_management.sql
-- ใน Supabase SQL Editor หรือ psql
```

### **2. ตรวจสอบการสร้างตาราง**
1. ไปที่หน้า **"จัดการเอกสารและไฟล์"**
2. ระบบจะสร้างตารางและข้อมูลเริ่มต้นอัตโนมัติ

## การแก้ไขปัญหา

### **ปัญหาการอัปโหลดไฟล์**
- ตรวจสอบขนาดไฟล์
- ตรวจสอบประเภทไฟล์ที่อนุญาต
- ตรวจสอบสิทธิ์การเขียนไฟล์

### **ปัญหาการแสดงผล**
- ตรวจสอบการเชื่อมต่อฐานข้อมูล
- ตรวจสอบ RLS policies
- ตรวจสอบ Console errors

### **ปัญหาการค้นหา**
- ตรวจสอบการสร้าง indexes
- ตรวจสอบการกรองข้อมูล
- ตรวจสอบการ pagination

## การพัฒนาต่อ

### **ฟีเจอร์ที่สามารถเพิ่มได้**
- ✅ การอัปโหลดไฟล์หลายไฟล์พร้อมกัน
- ✅ การบีบอัดไฟล์อัตโนมัติ
- ✅ การสร้าง thumbnail สำหรับรูปภาพ
- ✅ การแปลงไฟล์ PDF เป็นรูปภาพ
- ✅ การแชร์ไฟล์ผ่านลิงก์
- ✅ การตั้งเวลาลบไฟล์อัตโนมัติ
- ✅ การสำรองข้อมูลไฟล์
- ✅ การซิงค์กับ Cloud Storage

## การทดสอบ

### **ทดสอบการอัปโหลด**
1. อัปโหลดไฟล์ประเภทต่างๆ
2. ตรวจสอบการบันทึกในฐานข้อมูล
3. ตรวจสอบการแสดงผลในหน้า

### **ทดสอบการค้นหา**
1. ค้นหาตามชื่อผู้อัปโหลด
2. กรองตามหมวดหมู่
3. ตรวจสอบผลลัพธ์

### **ทดสอบการจัดการสิทธิ์**
1. สร้างไฟล์สาธารณะและส่วนตัว
2. ตรวจสอบการเข้าถึง
3. ตรวจสอบการติดตามการดาวน์โหลด
