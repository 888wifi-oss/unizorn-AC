# 🔧 แก้ไขปัญหาการนำเข้า Excel - ข้อมูล Owner และ Tenant ไม่แสดง

## ปัญหา
ข้อมูลส่วน "ไม่มีเจ้าของ", "ไม่มีผู้เช่า", และสถานะ "ว่าง" แสดงไม่ถูกต้อง ไม่ตรงกับข้อมูลใน Excel

## สาเหตุ
- Import เพิ่มข้อมูลในตาราง owners และ tenants แล้ว แต่ไม่ได้ผูก `current_owner_id` และ `current_tenant_id` ในตาราง units
- หน้าดึงข้อมูลจาก `unit.current_owner` และ `unit.current_tenant` ซึ่งอิงจาก `current_owner_id` และ `current_tenant_id`

## การแก้ไขที่ทำ

### 1. อัปเดตฟิลด์ current_owner_id และ current_tenant_id
```typescript
// เพิ่ม ownerId และ tenantId
let ownerId: string | undefined
let tenantId: string | undefined

// เก็บ owner ID หลังจาก insert สำเร็จ
ownerId = owner?.id

// เก็บ tenant ID หลังจาก insert สำเร็จ
tenantId = tenant?.id

// อัปเดต unit ด้วย owner และ tenant IDs
updateData.current_owner_id = ownerId
updateData.current_tenant_id = tenantId
```

### 2. เพิ่ม Logging
- แสดงข้อมูลที่กำลัง import
- แสดงผลการ insert owner และ tenant
- แสดงผลการ update unit

## 🔍 วิธีตรวจสอบ

### 1. ดู Log ใน Console
เปิด Developer Console (F12) และดู log:
```
[bulkImportUnits] Unit inserted successfully: UUID
[bulkImportUnits] Owner inserted successfully: UUID
[bulkImportUnits] Tenant inserted successfully: UUID
[bulkImportUnits] Unit updated with owner and tenant IDs: {...}
```

### 2. ตรวจสอบข้อมูลใน Database
รัน SQL เพื่อตรวจสอบ:
```sql
-- ตรวจสอบ units
SELECT id, unit_number, current_owner_id, current_tenant_id, status 
FROM units 
WHERE unit_number IN ('1005', '2006', '2007', '2008', '2009');

-- ตรวจสอบ owners
SELECT id, unit_id, name, email 
FROM owners 
WHERE unit_id IN (
  SELECT id FROM units WHERE unit_number IN ('1005', '2006', '2007', '2008', '2009')
);

-- ตรวจสอบ tenants
SELECT id, unit_id, name, email, status
FROM tenants 
WHERE unit_id IN (
  SELECT id FROM units WHERE unit_number IN ('1005', '2006', '2007', '2008', '2009')
);
```

## 📝 วิธีทดสอบใหม่

### 1. ลบข้อมูลที่ import ผิดพลาด
- ลบข้อมูลที่นำเข้าไปก่อนหน้า
- หรือทดสอบโดยใช้เลขห้องใหม่

### 2. นำเข้าข้อมูลใหม่อีกครั้ง
- เปิดหน้า Units
- คลิก "นำเข้าจาก Excel"
- เลือกไฟล์ Excel เดิม
- คลิก "ยืนยันการนำเข้า"

### 3. ตรวจสอบผลลัพธ์
- ดูข้อมูลในตารางว่าแสดง:
  - เจ้าของ: สมหญิง รักดี
  - ผู้เช่า: ผู้เช่า X (ถ้ามี)
  - สถานะ: ตามข้อมูลใน Excel

## ✨ ผลลัพธ์ที่คาดหวัง
- ✅ แสดงชื่อเจ้าของ: สมหญิง รักดี (ไม่ใช่ "ไม่มีเจ้าของ")
- ✅ แสดงชื่อผู้เช่า: ผู้เช่า 5-9 (ไม่ใช่ "ไม่มีผู้เช่า")
- ✅ แสดงสถานะ: "ว่าง" หรือ "มีผู้เช่า" (ไม่ใช่ "ว่าง" ทั้งหมด)


















