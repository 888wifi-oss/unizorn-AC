# ระบบออกบิลรายเดือน: ผู้รับบิลและระบบมิเตอร์

## 📋 สรุปการปรับปรุง

ระบบออกบิลรายเดือนได้รับการปรับปรุงให้รองรับ:
1. **การเลือกผู้รับบิล** - สามารถระบุได้ว่าบิลแต่ละประเภทส่งให้เจ้าของหรือผู้เช่า
2. **ระบบจดเลขมิเตอร์** - จดเลขมิเตอร์น้ำ/ไฟและคำนวณค่าใช้จ่ายอัตโนมัติ

---

## 🎯 ฟีเจอร์ใหม่

### 1. Bill Recipients (ผู้รับบิล)

ระบบสามารถระบุผู้รับบิลแต่ละประเภทได้:

#### ค่าส่วนกลาง (Common Fee)
- **Default**: ส่งให้เจ้าของ (Owner)
- **กรณีห้องปล่อยเช่า**: สามารถเลือกส่งให้ผู้เช่าได้
- **Auto Mode**: ส่งให้ผู้เช่าถ้ามีผู้เช่า, ไม่เช่นนั้นส่งให้เจ้าของ

#### ค่าน้ำ (Water Fee)
- **Default**: ส่งให้ผู้เช่า (Tenant) 
- **กรณีไม่มีผู้เช่า**: ส่งให้เจ้าของ
- **Auto Mode**: ส่งให้ผู้เช่าถ้ามี, ไม่เช่นนั้นส่งให้เจ้าของ

#### ค่าไฟ (Electricity Fee)
- **Default**: ส่งให้ผู้เช่า (Tenant)
- **กรณีไม่มีผู้เช่า**: ส่งให้เจ้าของ
- **Auto Mode**: ส่งให้ผู้เช่าถ้ามี, ไม่เช่นนั้นส่งให้เจ้าของ

#### ค่าจอดรถ (Parking Fee)
- **Default**: ส่งให้เจ้าของ (Owner)
- สามารถเลือกส่งให้ผู้เช่าได้

---

### 2. ระบบมิเตอร์ (Meter Reading System)

#### โครงสร้าง
- **Utility Meters**: จัดเก็บข้อมูลมิเตอร์น้ำ/ไฟแต่ละห้อง
- **Meter Readings**: บันทึกการอ่านเลขมิเตอร์แต่ละครั้ง
- **Utility Rates**: กำหนดอัตราค่าน้ำ/ค่าไฟ

#### การทำงาน
1. **จดเลขมิเตอร์**: บันทึกเลขมิเตอร์ปัจจุบัน
2. **ดึงเลขเก่า**: ระบบจะดึงเลขมิเตอร์ครั้งล่าสุดอัตโนมัติ
3. **คำนวณการใช้งาน**: `usage = current_reading - previous_reading`
4. **คำนวณค่าใช้จ่าย**: `cost = usage * rate_per_unit`
5. **เชื่อมโยงกับบิล**: เชื่อมโยง meter reading กับบิลที่สร้าง

---

## 📁 ไฟล์ที่สร้าง/แก้ไข

### 1. Database Migration
**`scripts/175_billing_recipients_and_meters.sql`**
- เพิ่ม columns สำหรับ bill recipients ใน `bills` table
- สร้าง/อัปเดต `utility_meters`, `meter_readings`, `utility_rates` tables
- สร้าง helper functions สำหรับคำนวณ
- สร้าง view `bills_with_recipients`

### 2. Helper Functions
**`lib/utils/meter-calculations.ts`**
- `getPreviousMeterReading()` - ดึงเลขมิเตอร์ครั้งล่าสุด
- `calculateUtilityCost()` - คำนวณค่าใช้จ่ายจาก usage
- `createMeterReading()` - สร้าง meter reading record
- `getActiveMeter()` - ดึงมิเตอร์ที่ใช้งานของห้อง
- `getLatestMeterReading()` - ดึงการอ่านมิเตอร์ล่าสุด

### 3. UI Components
**`app/(admin)/billing/batch-billing-dialog.tsx`**
- Dialog สำหรับการออกบิลรายเดือน
- เลือกผู้รับบิลแต่ละประเภท
- ตั้งค่าการจดเลขมิเตอร์

### 4. API Endpoint
**`app/api/v1/billing/batch-create/route.ts`**
- API สำหรับสร้างบิลรายเดือนแบบ batch
- รองรับการตั้งค่า recipients
- รองรับการจดเลขมิเตอร์และคำนวณค่าใช้จ่าย

---

## 🚀 ขั้นตอนการใช้งาน

### 1. รัน Migration Script
```sql
-- ใน Supabase SQL Editor
\i scripts/175_billing_recipients_and_meters.sql
```

### 2. ตั้งค่าน้ำ/ค่าไฟ (Utility Rates)
```sql
-- ตัวอย่าง: ตั้งอัตราค่าน้ำ
INSERT INTO utility_rates (
  meter_type, 
  rate_name, 
  rate_per_unit, 
  minimum_charge, 
  effective_date,
  is_active
) VALUES (
  'water',
  'ค่าน้ำประปา 2024',
  15.00,  -- 15 บาทต่อลูกบาศก์เมตร
  100.00, -- ค่าบริการขั้นต่ำ 100 บาท
  '2024-01-01',
  true
);

-- ตัวอย่าง: ตั้งอัตราค่าไฟ
INSERT INTO utility_rates (
  meter_type, 
  rate_name, 
  rate_per_unit, 
  minimum_charge, 
  effective_date,
  is_active
) VALUES (
  'electricity',
  'ค่าไฟฟ้า 2024',
  4.50,   -- 4.50 บาทต่อ kWh
  150.00, -- ค่าบริการขั้นต่ำ 150 บาท
  '2024-01-01',
  true
);
```

### 3. ลงทะเบียนมิเตอร์ (ถ้ายังไม่มี)
```sql
-- ตัวอย่าง: ลงทะเบียนมิเตอร์น้ำ
INSERT INTO utility_meters (
  unit_id,
  meter_type,
  meter_number,
  meter_location,
  is_active
) VALUES (
  '[unit_id]',
  'water',
  'WM-001',
  'หน้าห้องชุด',
  true
);

-- ตัวอย่าง: ลงทะเบียนมิเตอร์ไฟฟ้า
INSERT INTO utility_meters (
  unit_id,
  meter_type,
  meter_number,
  meter_location,
  is_active
) VALUES (
  '[unit_id]',
  'electricity',
  'EM-001',
  'หน้าห้องชุด',
  true
);
```

### 4. ใช้ระบบออกบิลรายเดือน

1. ไปที่หน้า **"ออกบิล"**
2. คลิกปุ่ม **"สร้างบิลรายเดือน"**
3. เลือกเดือนที่ต้องการสร้างบิล
4. ตั้งค่าผู้รับบิลแต่ละประเภท:
   - **Auto (แนะนำ)**: ระบบจะเลือกผู้รับบิลให้อัตโนมัติ
   - **Owner**: ส่งให้เจ้าของเสมอ
   - **Tenant**: ส่งให้ผู้เช่าเสมอ
5. เลือก **"รวมการจดเลขมิเตอร์"** (ถ้าต้องการ)
   - ระบุวันที่จดเลขมิเตอร์
6. คลิก **"ยืนยันการสร้างบิล"**

---

## 📊 ตัวอย่างการใช้งาน

### กรณีที่ 1: ห้องมีผู้เช่า

**ตั้งค่า Recipients:**
- ค่าส่วนกลาง: **Owner** (ส่งให้เจ้าของ)
- ค่าน้ำ: **Auto** → ส่งให้ **Tenant** (ผู้เช่า)
- ค่าไฟ: **Auto** → ส่งให้ **Tenant** (ผู้เช่า)
- ค่าจอดรถ: **Owner** (ส่งให้เจ้าของ)

**ผลลัพธ์:**
- เจ้าของได้รับบิล: ค่าส่วนกลาง + ค่าจอดรถ
- ผู้เช่าได้รับบิล: ค่าน้ำ + ค่าไฟ

---

### กรณีที่ 2: ห้องไม่มีผู้เช่า (เจ้าของอยู่เอง)

**ตั้งค่า Recipients:**
- ค่าส่วนกลาง: **Auto** → ส่งให้ **Owner**
- ค่าน้ำ: **Auto** → ส่งให้ **Owner**
- ค่าไฟ: **Auto** → ส่งให้ **Owner**
- ค่าจอดรถ: **Auto** → ส่งให้ **Owner**

**ผลลัพธ์:**
- เจ้าของได้รับบิลทั้งหมด

---

## 🔍 การจดเลขมิเตอร์

### ขั้นตอนการจดเลขมิเตอร์

1. **ก่อนสร้างบิล**: 
   - ไปที่หน้า "จัดการมิเตอร์" (จะสร้างภายหลัง)
   - จดเลขมิเตอร์น้ำ/ไฟของแต่ละห้อง

2. **ขณะสร้างบิล**:
   - เลือก "รวมการจดเลขมิเตอร์"
   - ระบุวันที่จดเลขมิเตอร์
   - ระบบจะคำนวณ:
     - `usage = current_reading - previous_reading`
     - `cost = usage * rate_per_unit`
     - อัปเดตค่า `water_fee` และ `electricity_fee` ในบิล

### ตัวอย่างการคำนวณ

```
เลขมิเตอร์น้ำ:
- เลขเก่า (เดือนที่แล้ว): 100 ลูกบาศก์เมตร
- เลขใหม่ (เดือนนี้): 120 ลูกบาศก์เมตร
- การใช้งาน: 120 - 100 = 20 ลูกบาศก์เมตร
- อัตรา: 15 บาท/ลูกบาศก์เมตร
- ค่าน้ำ: 20 * 15 = 300 บาท

เลขมิเตอร์ไฟฟ้า:
- เลขเก่า: 500 kWh
- เลขใหม่: 650 kWh
- การใช้งาน: 650 - 500 = 150 kWh
- อัตรา: 4.50 บาท/kWh
- ค่าไฟ: 150 * 4.50 = 675 บาท
```

---

## 📋 Database Schema

### Bills Table (New Columns)
```sql
-- Recipients
common_fee_recipient_type VARCHAR(20)  -- 'owner' or 'tenant'
common_fee_recipient_id UUID           -- owner_id or tenant_id
water_fee_recipient_type VARCHAR(20)
water_fee_recipient_id UUID
electricity_fee_recipient_type VARCHAR(20)
electricity_fee_recipient_id UUID
parking_fee_recipient_type VARCHAR(20)
parking_fee_recipient_id UUID

-- Meter Readings
water_meter_reading_id UUID            -- Reference to meter_readings
electricity_meter_reading_id UUID      -- Reference to meter_readings
```

### Utility Meters Table
```sql
id UUID PRIMARY KEY
unit_id UUID REFERENCES units(id)
meter_type VARCHAR(50)                  -- 'water', 'electricity', 'gas'
meter_number VARCHAR(100)
meter_location VARCHAR(255)
is_active BOOLEAN
```

### Meter Readings Table
```sql
id UUID PRIMARY KEY
meter_id UUID REFERENCES utility_meters(id)
reading_date DATE
previous_reading NUMERIC
current_reading NUMERIC
usage_amount NUMERIC                   -- calculated
unit_of_measure VARCHAR(20)             -- 'cubic_meter', 'kwh'
reading_type VARCHAR(50)                -- 'regular', 'estimated', 'corrected'
bill_id UUID REFERENCES bills(id)       -- Link to bill
```

### Utility Rates Table
```sql
id UUID PRIMARY KEY
meter_type VARCHAR(50)                  -- 'water', 'electricity'
rate_name VARCHAR(100)
rate_per_unit NUMERIC
minimum_charge NUMERIC
effective_date DATE
expiry_date DATE
is_active BOOLEAN
```

---

## 🛠️ Helper Functions

### `get_previous_meter_reading(meter_id, reading_date)`
ดึงเลขมิเตอร์ครั้งล่าสุดก่อนวันที่กำหนด

### `calculate_utility_cost(meter_type, usage_amount, reading_date)`
คำนวณค่าใช้จ่ายจากปริมาณการใช้และอัตรา

### `get_bill_recipient_info(recipient_type, recipient_id)`
ดึงข้อมูลผู้รับบิล (ชื่อ, อีเมล, เบอร์โทร)

---

## 📊 Views

### `bills_with_recipients`
View สำหรับดูบิลพร้อมข้อมูลผู้รับบิลและมิเตอร์

```sql
SELECT * FROM bills_with_recipients
WHERE month = '2024-01';
```

---

## ⚠️ หมายเหตุ

1. **Auto Mode**: 
   - ระบบจะตรวจสอบว่าห้องมีผู้เช่าหรือไม่
   - ถ้ามีผู้เช่า → ส่งให้ผู้เช่า (สำหรับค่าน้ำ/ค่าไฟ)
   - ถ้าไม่มีผู้เช่า → ส่งให้เจ้าของ

2. **Meter Readings**:
   - ต้องจดเลขมิเตอร์ก่อนสร้างบิล
   - ระบบจะดึงเลขเก่าอัตโนมัติ
   - ถ้าไม่มีเลขเก่า จะใช้ 0 เป็นค่าเริ่มต้น

3. **Utility Rates**:
   - ต้องตั้งค่า rates ก่อนใช้งาน
   - ระบบจะใช้ rate ที่ active และมีผลในวันที่อ่านมิเตอร์

---

## 🔮 Future Improvements

- [ ] หน้า UI สำหรับจัดการมิเตอร์ (จดเลข, แก้ไข, ดูประวัติ)
- [ ] ระบบแจ้งเตือนเมื่อเลขมิเตอร์ผิดปกติ
- [ ] Export รายงานการอ่านมิเตอร์
- [ ] รองรับการอ่านมิเตอร์ผ่าน API/Mobile App
- [ ] ระบบคำนวณค่าไฟแบบขั้นบันได (Tiered Pricing)

---

**วันที่สร้าง**: 2024-01-XX  
**เวอร์ชัน**: 1.0.0

