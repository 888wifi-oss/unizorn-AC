771# 🔧 แก้ไขปัญหาการเข้าสู่ระบบไม่ได้ - ไม่พบผู้ใช้ในระบบ

## ปัญหา
หลังจากรัน SQL script แล้ว เข้าระบบไม่ได้ และแสดงข้อความ "ไม่พบผู้ใช้ในระบบ"

## สาเหตุที่เป็นไปได้
1. **ตาราง owners หรือ tenants ไม่มีข้อมูล** - SQL script พยายาม JOIN ตารางที่ไม่มีข้อมูล
2. **ตาราง owners หรือ tenants ไม่มีอยู่** - ตารางยังไม่ได้ถูกสร้าง
3. **ข้อมูล username หรือ password ไม่ถูกต้อง** - ข้อมูลในตาราง units ไม่ถูกต้อง

## วิธีแก้ไข

### ขั้นตอนที่ 1: ตรวจสอบข้อมูล
```sql
-- รันไฟล์ scripts/160_debug_resident_login_issue.sql
-- เพื่อตรวจสอบข้อมูลในตารางต่างๆ
```

### ขั้นตอนที่ 2: ตรวจสอบข้อมูลในตาราง units
```sql
-- ตรวจสอบว่ามี username และ password หรือไม่
SELECT 
  unit_number,
 0 username,
  password,
  owner_name,
  resident_name
FROM units 
WHERE username IS NOT NULL;
```

### ขั้นตอนที่ 3: ตรวจสอบข้อมูลในตาราง owners
```sql
-- ตรวจสอบว่ามีข้อมูลในตาราง owners หรือไม่
SELECT 
  unit_id,
  name,
  is_primary,
  email
FROM owners;
```

### ขั้นตอนที่ 4: ตรวจสอบข้อมูลในตาราง tenants
```sql
-- ตรวจสอบว่ามีข้อมูลในตาราง tenants หรือไม่
SELECT 
  unit_id,
  name,
  status,
  email
FROM tenants;
```

## การแก้ไขที่ทำ

### 1. แก้ไขโค้ด Authentication
- **`lib/supabase/ultra-simple-auth.ts`** - แก้ไขให้ทำงานได้แม้ว่าจะไม่มีข้อมูลในตาราง owners หรือ tenants
- ใช้ try-catch เพื่อจัดการ error
- ดึงข้อมูลจากตาราง owners และ tenants แยกกัน

### 2. ลำดับการดึงชื่อ
1. **`resident_name`** (ถ้ามี) - จากตาราง units
2. **`owners.name`** (เจ้าของหลัก) - จากตาราง owners
3. **`tenants.name`** (ผู้เช่าที่ active) - จากตาราง tenants
4. **`owner_name`** (ชื่อเจ้าของห้อง) - จากตาราง units
5. **"ผู้อาศัยห้อง [หมายเลขห้อง]"** - ชื่อ generic

## การทดสอบ

### 1. ทดสอบการเข้าสู่ระบบ
- ใช้ username และ password ที่มีในตาราง units
- ตรวจสอบ console log เพื่อดู error

### 2. ตรวจสอบข้อมูล
- ตรวจสอบว่ามีข้อมูลในตาราง units หรือไม่
- ตรวจสอบว่ามีข้อมูลในตาราง owners หรือ tenants หรือไม่

## หมายเหตุสำคัญ
- ระบบจะทำงานได้แม้ว่าจะไม่มีข้อมูลในตาราง owners หรือ tenants
- ถ้าไม่มีข้อมูลในตาราง owners หรือ tenants จะใช้ชื่อจากตาราง units
- ระบบจะแสดง error ใน console log เพื่อช่วยในการ debug

## ไฟล์ที่แก้ไข
- **`lib/supabase/ultra-simple-auth.ts`** - แก้ไขการดึงข้อมูลชื่อ
- **`scripts/160_debug_resident_login_issue.sql`** - SQL script สำหรับตรวจสอบข้อมูล
