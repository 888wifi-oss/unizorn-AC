# 🔧 แก้ไขปัญหาการแสดงชื่อผู้ลงทะเบียนจริง

## ปัญหา
1. **ผู้เช่าเป็นคนลงทะเบียน** แต่ระบบแสดงชื่อเจ้าของห้อง
2. **ต้องดึงชื่อของผู้ที่ลงทะเบียนจริง** ไม่ใช่เจ้าของห้อง

## สาเหตุที่ถูกต้อง
- ระบบใช้ `username` ในตาราง `units` สำหรับการเข้าสู่ระบบ
- แต่ `owner_name` เป็นชื่อเจ้าของห้อง ไม่ใช่ชื่อของผู้ที่ลงทะเบียนจริง
- ใน 1 ห้องอาจมีผู้อาศัยหลายคน แต่ละคนมี username ต่างกัน
- **ต้องระบุว่าใครเป็นคนลงทะเบียนด้วย username นั้น**

## วิธีแก้ไขที่ถูกต้อง

### 1. รัน SQL Script ใน Supabase
```sql
-- รันไฟล์ scripts/161_fix_registered_resident_name.sql
```

### 2. การทำงานของระบบใหม่
ระบบจะดึงชื่อตามลำดับความสำคัญ:
1. **`resident_name`** (ถ้ามี) - ชื่อที่อัปเดตในตาราง units
2. **`registered_by`** (ผู้ลงทะเบียน) - หาจาก owners หรือ tenants ที่มีข้อมูลตรงกับ registered_by
3. **`owners.name`** (เจ้าของหลัก) - จากตาราง owners ที่ is_primary = true
4. **`tenants.name`** (ผู้เช่าที่ active) - จากตาราง tenants ที่ status = 'active'
5. **`owner_name`** (ชื่อเจ้าของห้อง) - จากตาราง units
6. **"ผู้อาศัยห้อง [หมายเลขห้อง]"** - ชื่อ generic

### 3. ไฟล์ที่แก้ไข
1. **`lib/supabase/ultra-simple-auth.ts`** - แก้ไขการดึงข้อมูลชื่อผู้ลงทะเบียนจริง
2. **`app/portal/dashboard/page.tsx`** - แก้ไขการแสดงชื่อ
3. **`app/portal/login/page.tsx`** - แก้ไขข้อความต้อนรับ

## การทดสอบ

### 1. ทดสอบการเข้าสู่ระบบ
- ใช้ username และ password ที่มีในตาราง units
- ตรวจสอบว่าชื่อที่แสดงเป็นชื่อของผู้ที่ลงทะเบียนจริง

### 2. ตรวจสอบข้อมูล
- ตรวจสอบว่ามีข้อมูลในตาราง owners หรือ tenants หรือไม่
- ตรวจสอบว่าฟิลด์ `registered_by` ถูกตั้งค่าถูกต้องหรือไม่

## หมายเหตุสำคัญ
- ระบบจะดึงชื่อจากตาราง `owners` และ `tenants` โดยอัตโนมัติ
- ฟิลด์ `registered_by` จะถูกตั้งค่าเป็น `username` โดยอัตโนมัติ
- ใน 1 ห้องอาจมีผู้อาศัยหลายคน แต่ละคนมี username และชื่อต่างกัน
- ระบบจะทำงานได้ทันทีหลังจากรัน SQL script

## ไฟล์ SQL Script
- **`scripts/161_fix_registered_resident_name.sql`** - SQL script ที่แก้ไขปัญหาการแสดงชื่อผู้ลงทะเบียนจริง

## ตัวอย่างการทำงาน
- ถ้า username = 'ADD' และมี tenant ชื่อ 'อานนท์ ดีใจ' ในตาราง tenants
- ระบบจะแสดง "ยินดีต้อนรับ, อานนท์ ดีใจ" (ชื่อผู้เช่าที่ลงทะเบียน)
- ไม่ใช่ "ยินดีต้อนรับ, เจ้าของห้อง 1001" (ชื่อเจ้าของห้อง)
















