# การปรับปรุงระบบออกบิลให้สอดคล้องกับผังบัญชี

## 📋 สรุปการปรับปรุง

ระบบออกบิลได้ถูกปรับปรุงให้เชื่อมโยงกับผังบัญชี (Chart of Accounts) โดยอัตโนมัติ เพื่อให้การบันทึกรายได้ไปยัง `revenue_journal` ใช้ account codes ที่ถูกต้องตามมาตรฐานบัญชี

---

## 🎯 Account Code Mapping

### รายได้จากการดำเนินงาน (4100)
- **4101** - รายได้ค่าส่วนกลาง (`common_fee`)
- **4102** - รายได้ค่าน้ำ (`water_fee`)
- **4103** - รายได้ค่าไฟฟ้า (`electricity_fee`)
- **4104** - รายได้ค่าที่จอดรถ (`parking_fee`)

### รายได้อื่น (4200)
- **4203** - รายได้เบ็ดเตล็ด (`other_fee`)

---

## 📁 ไฟล์ที่สร้าง/แก้ไข

### 1. Migration Script
**`scripts/173_billing_chart_of_accounts_integration.sql`**
- สร้างฟังก์ชัน `get_account_code_for_bill_item()` สำหรับ mapping
- สร้างฟังก์ชัน `create_revenue_journal_from_bill()` สำหรับสร้าง journal entries
- สร้าง trigger `trg_create_revenue_journal_on_bill` เพื่อ auto-create journal entries
- สร้าง view `bills_with_accounts` สำหรับดูบิลพร้อม account mapping
- เพิ่ม indexes สำหรับ performance

### 2. Helper Functions
**`lib/utils/billing-accounts.ts`**
- `getAccountCodeForBillItem()` - แปลง bill item type เป็น account code
- `createRevenueJournalEntries()` - สร้าง revenue journal entries จากบิล
- `getAccountCodeMapping()` - ดู mapping ทั้งหมด
- Constants: `BILLING_ACCOUNT_CODES`

### 3. Actions
**`lib/supabase/actions.ts`**
- ปรับปรุง `saveBillToDB()` ให้สร้าง revenue journal entries อัตโนมัติ
- ปรับปรุง `deleteBillFromDB()` ให้ลบ revenue journal entries เมื่อลบบิล

### 4. UI
**`app/(admin)/billing/page.tsx`**
- แสดง account codes ในตารางบิล (ใต้จำนวนเงินแต่ละรายการ)
- แสดง account codes ในฟอร์มสร้าง/แก้ไขบิล
- Import helper functions สำหรับการแสดงผล

---

## 🔄 การทำงาน

### เมื่อสร้างบิลใหม่:
1. บันทึกบิลใน `bills` table
2. Trigger `trg_create_revenue_journal_on_bill` จะทำงานอัตโนมัติ
3. สร้าง revenue journal entries หลายรายการ (ตามจำนวน fee types ที่มีค่า > 0)
4. แต่ละ entry จะมี `account_code` ที่ถูกต้องตามผังบัญชี

### เมื่อแก้ไขบิล:
1. อัปเดตข้อมูลใน `bills` table
2. ลบ revenue journal entries เก่าที่เชื่อมโยงกับบิลนี้
3. สร้าง revenue journal entries ใหม่ด้วยข้อมูลที่อัปเดต

### เมื่อลบบิล:
1. ลบ revenue journal entries ที่เกี่ยวข้อง
2. ลบบิล

---

## 📊 ตัวอย่าง Revenue Journal Entries

เมื่อสร้างบิลที่มี:
- ค่าส่วนกลาง: 2,000 บาท
- ค่าน้ำ: 300 บาท
- ค่าไฟ: 500 บาท

ระบบจะสร้าง 3 journal entries:

```sql
-- Entry 1
journal_date: '2024-01-01'
account_code: '4101' (รายได้ค่าส่วนกลาง)
amount: 2000
bill_id: [bill_id]

-- Entry 2
journal_date: '2024-01-01'
account_code: '4102' (รายได้ค่าน้ำ)
amount: 300
bill_id: [bill_id]

-- Entry 3
journal_date: '2024-01-01'
account_code: '4103' (รายได้ค่าไฟฟ้า)
amount: 500
bill_id: [bill_id]
```

---

## ✅ ประโยชน์

1. **ความถูกต้องทางการบัญชี**: Account codes ตรงกับผังบัญชี
2. **อัตโนมัติ**: ไม่ต้องบันทึก journal entries เอง
3. **ตรวจสอบได้**: สามารถดูบิลพร้อม account codes ได้จาก view `bills_with_accounts`
4. **สอดคล้องกับระบบ**: ข้อมูลใน `revenue_journal` และรายงานทางการเงินจะถูกต้อง

---

## 🚀 ขั้นตอนการใช้งาน

### 1. รัน Migration Script
```bash
# ใน Supabase SQL Editor หรือ psql
\i scripts/173_billing_chart_of_accounts_integration.sql
```

### 2. ตรวจสอบผลลัพธ์
- ตรวจสอบว่ามี functions และ triggers ถูกสร้างแล้ว
- ทดสอบสร้างบิลใหม่ และดู revenue journal entries ที่ถูกสร้าง

### 3. ใช้ระบบออกบิล
- สร้าง/แก้ไขบิลตามปกติ
- ระบบจะสร้าง revenue journal entries อัตโนมัติ
- Account codes จะแสดงในตารางและฟอร์ม

---

## 🔍 การตรวจสอบ

### ตรวจสอบ Revenue Journal Entries:
```sql
SELECT 
  rj.*,
  coa.account_name,
  b.bill_number,
  u.unit_number
FROM revenue_journal rj
JOIN chart_of_accounts coa ON rj.account_code = coa.account_code
LEFT JOIN bills b ON rj.bill_id = b.id
LEFT JOIN units u ON rj.unit_id = u.id
WHERE rj.bill_id = '[bill_id]'
ORDER BY rj.account_code;
```

### ดูบิลพร้อม Account Mapping:
```sql
SELECT * FROM bills_with_accounts
WHERE id = '[bill_id]';
```

---

## ⚠️ หมายเหตุ

1. **Backward Compatibility**: บิลเก่าที่สร้างก่อน migration จะไม่มี revenue journal entries อัตโนมัติ
   - สามารถรัน `create_revenue_journal_from_bill()` manual สำหรับบิลเก่าได้

2. **Trigger vs Manual**: 
   - Trigger จะทำงานอัตโนมัติ
   - แต่ `saveBillToDB()` ก็เรียก manual backup เพื่อความแน่ใจ

3. **Error Handling**: 
   - หากเกิด error ในการสร้าง journal entries จะไม่ทำให้การบันทึกบิลล้มเหลว
   - Error จะถูก log ใน console

---

## 📝 TODO / Future Improvements

- [ ] เพิ่ม UI สำหรับ sync บิลเก่า (สร้าง revenue journal entries สำหรับบิลที่สร้างก่อน migration)
- [ ] เพิ่ม validation เพื่อตรวจสอบว่า account code มีอยู่ในผังบัญชี
- [ ] เพิ่มการแสดงชื่อบัญชี (account_name) ในตารางบิล
- [ ] เพิ่ม report ที่แสดง mapping ระหว่างบิลกับ revenue journal

---

## 📞 ติดต่อ/สอบถาม

หากมีคำถามหรือพบปัญหา กรุณาติดต่อทีมพัฒนา

---

**วันที่ปรับปรุง**: 2024-01-XX
**เวอร์ชัน**: 1.0.0

