# 🔧 แก้ไขลำดับการแสดงชื่อ - แสดงชื่อผู้เช่าก่อนเจ้าของ

## ปัญหา
ระบบแสดงชื่อเจ้าของห้อง แม้ว่าจะมีการลงทะเบียนเป็นผู้เช่า

## สาเหตุ
ลำดับการหาชื่อไม่ถูกต้อง:
- ระบบหาชื่อจาก owners ก่อน
- ควรหาชื่อจาก tenants ก่อน (ผู้เช่าที่ active)

## การแก้ไขที่ทำ

### เปลี่ยนลำดับการหาชื่อใหม่:

1. **หาจาก tenants ก่อน** (ผู้เช่าที่ active) ← **เพิ่มก่อน**
2. **หาจาก owners** (เจ้าของหลัก)
3. **ใช้ resident_name** (ถ้ามี)
4. **ใช้ owner_name** (ชื่อเจ้าของห้อง)
5. **ใช้ชื่อ generic** ("ผู้อาศัยห้อง [หมายเลขห้อง]")

### ไฟล์ที่แก้ไข:
- **`lib/supabase/ultra-simple-auth.ts`** - แก้ไขลำดับการหาชื่อ

## การทดสอบ

### 1. ทดสอบการเข้าสู่ระบบ
- ใช้ username และ password ที่มีในตาราง units
- ตรวจสอบว่าชื่อที่แสดงเป็นชื่อผู้เช่าหรือเจ้าของห้อง

### 2. ตรวจสอบ Console Log
ระบบจะแสดง log ดังนี้:
```
[Ultra Simple] Found active tenant: [ชื่อผู้เช่า]
```
หรือ
```
[Ultra Simple] Found primary owner: [ชื่อเจ้าของ]
```

## ตัวอย่างการทำงาน

### กรณีที่ 1: มีผู้เช่าที่ active
- **ถ้า username = 'ADD'** และมี tenant ชื่อ **'อานนท์ ดีใจ'** ในตาราง tenants (status = 'active')
- ระบบจะแสดง **"ยินดีต้อนรับ, อานนท์ ดีใจ"** (ชื่อผู้เช่า)

### กรณีที่ 2: ไม่มีผู้เช่าที่ active
- **ถ้า username = 'ADD'** และมี owner ชื่อ **'สมชาย ใจดี'** ในตาราง owners (is_primary = true)
- ระบบจะแสดง **"ยินดีต้อนรับ, สมชาย ใจดี"** (ชื่อเจ้าของ)

## หมายเหตุสำคัญ
- ระบบจะตรวจสอบผู้เช่าที่ active ก่อน
- ถ้าไม่พบผู้เช่าที่ active จะแสดงชื่อเจ้าของห้อง
- ระบบจะแสดง log ใน console เพื่อช่วยในการ debug

## ผลลัพธ์
- **ถ้ามีผู้เช่าที่ active** จะแสดงชื่อผู้เช่า
- **ถ้าไม่มีผู้เช่าที่ active** จะแสดงชื่อเจ้าของห้อง
