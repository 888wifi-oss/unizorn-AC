# 🔧 แก้ไขปัญหาการนำเข้า Excel - ไม่มีข้อมูลเข้าในระบบ

## ปัญหา
ระบบแจ้งนำเข้าสำเร็จ แต่ไม่มีข้อมูลเข้าในระบบ

## สาเหตุที่เป็นไปได้
1. **ฟิลด์ในตาราง units ไม่ตรงกับข้อมูลที่ส่งเข้า** - ฟิลด์ `bedroom`, `bathroom`, `type` อาจไม่มีในตาราง
2. **project_id ไม่ถูกต้อง** - ระบบไม่รู้จัก project_id ที่ส่งมา
3. **ไฟล์ Excel ไม่ถูกต้อง** - โครงสร้างหรือข้อมูลไม่ตรงกับที่กำหนด

## การแก้ไขที่ทำ

### 1. แก้ไขการบันทึกข้อมูล
- ลบฟิลด์ `bedroom`, `bathroom`, `type` ที่อาจไม่มีในตาราง units
- เพิ่มฟิลด์ `residents: 1` แทน

### 2. เพิ่ม Logging
- เพิ่ม console.log เพื่อ debug
- แสดงข้อมูลที่กำลังส่งเข้า API
- แสดงผลการ import

### 3. แก้ไข Logic การสร้าง Owner และ Tenant
- สร้าง Owner เสมอ
- สร้าง Tenant เฉพาะเมื่อมีข้อมูล tenant_name

## 🔍 วิธีตรวจสอบปัญหา

### 1. เปิด Developer Console
- กด F12 ใน browser
- ดู Console tab
- ตรวจสอบ log ที่ขึ้นว่า:
  - `[bulkImportUnits] Starting bulk import with X units`
  - `[bulkImportUnits] Processing unit: XXX`
  - `[bulkImportUnits] Inserting unit with data: {...}`
  - `[bulkImportUnits] Unit inserted successfully: XXX`
  - `[bulkImportUnits] Import completed. Success: X Failed: X`

### 2. ตรวจสอบข้อมูลที่ส่งเข้า API
- ดู log `[Import] Starting bulk import with data:`
- ตรวจสอบว่า project_id ถูกต้องหรือไม่
- ตรวจสอบว่าข้อมูลครบถ้วนหรือไม่

### 3. ตรวจสอบ Error Messages
- ดู log `[bulkImportUnits] Error importing unit:`
- ตรวจสอบ error message ว่าอะไร

## 📝 วิธีทดสอบใหม่

### 1. โหลดหน้า Units
- เปิดหน้า `http://localhost:3000/units`
- เปิด Developer Console

### 2. ดาวน์โหลดเทมเพลต
- คลิก "นำเข้าจาก Excel"
- คลิก "ดาวน์โหลดเทมเพลต"
- เปิดไฟล์ template ที่ดาวน์โหลด

### 3. กรอกข้อมูล
- ใช้ข้อมูลจาก Excel ที่เห็นในภาพ
- ตัวอย่าง:
  - หมายเลขห้อง: 1005
  - ประเภท: คอนโด
  - ชั้น: 1
  - ขนาด (ตร.ม.): 45
  - ห้องนอน: 2
  - ห้องน้ำ: 1
  - เจ้าของ: สมหญิง รักดี
  - อีเมลเจ้าของ: somchai5@email.com
  - ผู้เช่า: ผู้เช่า 5
  - อีเมลผู้เช่า: tenant4@email.com
  - สถานะ: ว่าง
  - ราคาเช่า: 20000

### 4. นำเข้าข้อมูล
- เลือกไฟล์ Excel
- ตรวจสอบผลการตรวจสอบข้อมูล
- คลิก "ยืนยันการนำเข้า"
- ดู log ใน Console

## 🎯 ผลลัพธ์ที่คาดหวัง

### Log ที่ควรเห็น:
```
[Import] Starting bulk import with data: [...]
[bulkImportUnits] Starting bulk import with 5 units
[bulkImportUnits] Processing unit: 1005
[bulkImportUnits] Inserting unit with data: {...}
[bulkImportUnits] Unit inserted successfully: UUID
[bulkImportUnits] Unit imported successfully. Total success: 1
[bulkImportUnits] Import completed. Success: 5 Failed: 0
[Import] Import result: {success: true, imported: 5, failed: 0, errors: []}
```

### ข้อมูลที่ควรเห็น:
- มีข้อมูล 5 ห้องใหม่ในตาราง
- แต่ละห้องมี Owner
- ห้องที่มีผู้เช่ามี Tenant

## 📝 ไฟล์ที่แก้ไข
1. **`lib/actions/bulk-import-actions.ts`** - แก้ไขการ insert ข้อมูล และเพิ่ม logging
2. **`app/(admin)/units/page.tsx`** - แก้ไขการจัดการ import progress


















