# 🔧 แก้ไขปัญหาการแสดงชื่อลูกบ้าน - ดึงชื่อจากตาราง owners และ tenants

## ปัญหา
ระบบแสดงข้อความ "ยินดีต้อนรับ, เจ้าของห้อง 1001" แทนที่จะแสดงชื่อจริงของผู้อาศัยที่ลงทะเบียนด้วย username นั้น

## สาเหตุที่ถูกต้อง
- ระบบใช้ `username` ในตาราง `units` สำหรับการเข้าสู่ระบบ
- แต่ `owner_name` ในตาราง `units` เป็นชื่อเจ้าของห้อง ไม่ใช่ชื่อของผู้อาศัยที่ลงทะเบียนด้วย username นั้น
- ใน 1 ห้องอาจมีผู้อาศัยหลายคน แต่ละคนมี username ต่างกัน
- **ชื่อจริงของผู้อาศัยเก็บอยู่ในตาราง `owners` และ `tenants`** ไม่ใช่ในตาราง `units`

## วิธีแก้ไขที่ถูกต้อง

### 1. รัน SQL Script ใน Supabase
```sql
-- รันไฟล์ scripts/159_fix_resident_name_from_owners_tenants.sql
```

### 2. การทำงานของระบบ
ระบบจะดึงชื่อตามลำดับความสำคัญ:
1. **`resident_name`** (ถ้ามี) - ชื่อที่อัปเดตในตาราง units
2. **`owners.name`** (เจ้าของหลัก) - จากตาราง owners ที่ is_primary = true
3. **`tenants.name`** (ผู้เช่าที่ active) - จากตาราง tenants ที่ status = 'active'
4. **`owner_name`** (ชื่อเจ้าของห้อง) - จากตาราง units
5. **"ผู้อาศัยห้อง [หมายเลขห้อง]"** - ชื่อ generic

### 3. ไฟล์ที่แก้ไข
1. **`lib/supabase/ultra-simple-auth.ts`** - แก้ไขการดึงข้อมูลชื่อจากตาราง owners และ tenants
2. **`app/portal/dashboard/page.tsx`** - แก้ไขการแสดงชื่อ
3. **`app/portal/login/page.tsx`** - แก้ไขข้อความต้อนรับ

## การทดสอบ
1. เข้าสู่ระบบด้วย username และ password
2. ตรวจสอบว่าชื่อที่แสดงเป็นชื่อจริงของผู้อาศัยจากตาราง owners หรือ tenants
3. ถ้ายังแสดงชื่อ generic ให้ตรวจสอบข้อมูลในตาราง owners และ tenants

## หมายเหตุสำคัญ
- ระบบจะดึงชื่อจากตาราง `owners` และ `tenants` โดยอัตโนมัติ
- ไม่ต้องอัปเดตชื่อในตาราง `units` ด้วยตนเอง
- ใน 1 ห้องอาจมีผู้อาศัยหลายคน แต่ละคนมี username และชื่อต่างกัน
- ระบบจะทำงานได้ทันทีหลังจากรัน SQL script

## ไฟล์ SQL Script
- **`scripts/159_fix_resident_name_from_owners_tenants.sql`** - SQL script ที่ดึงชื่อจากตาราง owners และ tenants

## ตัวอย่างข้อมูล
จากภาพที่เห็น:
- ห้อง 1001: อำนวย (จากตาราง owners)
- ห้อง 1002: ทองคุณ (จากตาราง owners)
- ห้อง 1003: การดา (จากตาราง owners)
- ห้อง 1004: ดวงรัตน์ (จากตาราง owners)
- ห้อง 1005: ดวงดี (จากตาราง owners)
- ห้อง AAA-ABCD-A-101: AAA (จากตาราง owners)


















