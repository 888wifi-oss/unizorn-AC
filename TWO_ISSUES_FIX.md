# 🔧 **แก้ไขปัญหา 2 รายการ**

## **❌ ปัญหาที่พบ:**

### **1. สถานะไม่ถูกต้อง**
```
ระบุเจ้าของพักเอง แต่สถานะยังแสดง "ว่าง"
```

### **2. เพิ่มห้องเกิด Error**
```
[createUnit] Error: {
  code: '23502',
  message: 'null value in column "floor" of relation "units" violates not-null constraint'
}
```

---

## **✅ การแก้ไข:**

### **🔧 ปัญหาที่ 1: สถานะไม่ถูกต้อง**

#### **สาเหตุ:**
- `getUnits` function ไม่ได้โหลด `owner_occupies` จาก database
- ทำให้ `unit.current_owner.owner_occupies` เป็น `undefined`
- Logic การคำนวณสถานะจึงไม่ทำงาน

#### **การแก้ไข:**
```typescript
// lib/actions/units-actions.ts
current_owner:owners!current_owner_id(
  id,
  name,
  email,
  phone,
  national_id,
  is_primary,
  ownership_percentage,
  owner_occupies  // ✅ เพิ่มฟิลด์นี้
),
```

---

### **🔧 ปัญหาที่ 2: เพิ่มห้องเกิด Error**

#### **สาเหตุ:**
- `formData.floor` เป็น empty string `""`
- `parseInt("")` จะได้ `NaN`
- `NaN` ไม่สามารถบันทึกใน database ได้

#### **การแก้ไข:**
```typescript
// app/(admin)/units/page.tsx

// เพิ่มการตรวจสอบ required fields
if (!formData.floor || formData.floor.toString().trim() === '') {
  toast({
    title: "เกิดข้อผิดพลาด",
    description: "กรุณากรอกชั้น",
    variant: "destructive",
  })
  return
}

// เพิ่ม fallback values
floor: parseInt(formData.floor) || 0,
size: parseFloat(formData.size) || 0,
```

---

## **🎯 ขั้นตอนการแก้ไข:**

### **Step 1: รัน SQL Script**
```sql
-- รันไฟล์ scripts/145_add_owner_occupies_field.sql
-- เพื่อเพิ่มคอลัมน์ owner_occupies ในตาราง owners
```

### **Step 2: รีเฟรชหน้าเว็บ**
- กด F5 เพื่อรีเฟรชหน้าเว็บ
- ทำให้โค้ดใหม่โหลดเข้ามา

### **Step 3: ทดสอบการแก้ไขเจ้าของ**
1. ดูรายละเอียดห้องชุด
2. คลิก "แก้ไขเจ้าของ"
3. เช็ค "เจ้าของพักเอง (ไม่ได้ให้เช่า)"
4. กด "อัปเดต"
5. **สถานะควรจะเปลี่ยนเป็น "เจ้าของอยู่เอง"**

### **Step 4: ทดสอบการเพิ่มห้อง**
1. คลิก "เพิ่มห้องชุด"
2. กรอกข้อมูลห้องชุด
3. **ต้องกรอกชั้นและขนาด** (required fields)
4. กด "สร้าง"
5. **ควรสร้างสำเร็จโดยไม่มี error**

---

## **📊 Checklist:**

### **✅ สิ่งที่ต้องทำ:**
- [ ] รัน SQL script `scripts/145_add_owner_occupies_field.sql`
- [ ] รีเฟรชหน้าเว็บ (F5)
- [ ] ทดสอบแก้ไขเจ้าของ → สถานะควรเป็น "เจ้าของอยู่เอง"
- [ ] ทดสอบเพิ่มห้อง → ต้องกรอกชั้นและขนาด
- [ ] ทดสอบเพิ่มห้อง → ควรสร้างสำเร็จ

---

## **🎉 ผลลัพธ์:**

### **✅ ปัญหาที่แก้ไข:**
1. **สถานะไม่ถูกต้อง** - แก้ไขแล้ว ✅
2. **เพิ่มห้องเกิด Error** - แก้ไขแล้ว ✅

### **🚀 ฟีเจอร์ที่พร้อมใช้งาน:**
- แสดงสถานะห้องชุดที่แม่นยำจาก `owner_occupies`
- เพิ่มห้องชุดได้โดยไม่มี error
- ตรวจสอบ required fields ก่อนบันทึก
- แสดง error message ที่ชัดเจน

---

**🎯 ปัญหาทั้ง 2 รายการแก้ไขแล้ว! รีเฟรชหน้าเว็บแล้วลองใหม่อีกครั้ง!** 🚀
