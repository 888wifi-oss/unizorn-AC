# 🔧 แก้ไขการแสดงสถานะบัญชี - แสดงสถานะที่ถูกต้อง

## ปัญหา
กรณีลงทะเบียนสำเร็จแล้ว แต่สถานะยังแสดง "ยังไม่มีบัญชี"
ควรแสดงสถานะที่ถูกต้องหากบัญชีได้ทำการผูกแล้ว

## สาเหตุ
- ระบบตั้งค่า `has_account = false` โดยตรง
- ไม่ได้ตรวจสอบว่ามี username และ password หรือไม่

## การแก้ไขที่ทำ

### เปลี่ยนการตรวจสอบสถานะบัญชี:

**ก่อน:**
```typescript
has_account: false, // TODO: Check if owner has auth account
```

**หลัง:**
```typescript
// ตรวจสอบว่ามี username และ password หรือไม่
const hasAccount = !!(unit.username && unit.password)

has_account: hasAccount,
```

### ไฟล์ที่แก้ไข:
- **`app/(admin)/resident-accounts/page.tsx`** - แก้ไขการตรวจสอบสถานะบัญชี

## การทำงานของระบบ

### การตรวจสอบสถานะบัญชี:
1. ตรวจสอบว่ามี `username` และ `password` ในตาราง units
2. ถ้ามีทั้ง username และ password → `has_account = true`
3. ถ้าไม่มี username หรือ password → `has_account = false`

### สถานะที่แสดง:
- **"มีบัญชีแล้ว"** - ถ้ามี username และ password
- **"ยังไม่มีบัญชี"** - ถ้าไม่มี username หรือ password

## การทดสอบ

### 1. ทดสอบการแสดงสถานะ
- เปิดหน้า "จัดการบัญชีผู้ใช้ Portal"
- ตรวจสอบว่าสถานะแสดงถูกต้องหรือไม่

### 2. ทดสอบการสร้างบัญชี
- สร้างบัญชีใหม่
- ตรวจสอบว่าสถานะเปลี่ยนเป็น "มีบัญชีแล้ว" หรือไม่

## หมายเหตุสำคัญ
- ระบบจะตรวจสอบ username และ password โดยอัตโนมัติ
- ถ้าบัญชีถูกผูกแล้ว สถานะจะแสดง "มีบัญชีแล้ว"
- ถ้าบัญชียังไม่ถูกผูก สถานะจะแสดง "ยังไม่มีบัญชี"

## ผลลัพธ์
- แทนที่จะแสดง "ยังไม่มีบัญชี" ทั้งหมด
- จะแสดงสถานะที่ถูกต้องตามการผูกบัญชีจริง


















