# 🔧 แก้ไขปัญหาการแสดงชื่อลูกบ้าน

## ปัญหา
ระบบแสดงข้อความ "ยินดีต้อนรับ, เจ้าของห้อง 1001" แทนที่จะแสดงชื่อจริงของลูกบ้าน

## สาเหตุ
- ข้อมูล `owner_name` ในตาราง `units` เป็นค่า generic เช่น "เจ้าของห้อง 1001"
- ไม่มีฟิลด์สำหรับเก็บชื่อจริงของลูกบ้าน

## วิธีแก้ไข

### 1. รัน SQL Script ใน Supabase
```sql
-- รันไฟล์ FIX_RESIDENT_NAME_DISPLAY.sql ก่อน
-- จากนั้นรันไฟล์ UPDATE_REAL_RESIDENT_NAMES.sql
```

### 2. อัปเดตชื่อจริงของลูกบ้าน
แก้ไขไฟล์ `UPDATE_REAL_RESIDENT_NAMES.sql` โดยเปลี่ยนชื่อในส่วน:
```sql
WHEN unit_number = '1001' THEN 'ชื่อจริงของลูกบ้านห้อง 1001'
WHEN unit_number = '1002' THEN 'ชื่อจริงของลูกบ้านห้อง 1002'
-- เพิ่มตามจำนวนห้องที่มี
```

### 3. การทำงานของระบบ
- ระบบจะแสดง `resident_name` ก่อน
- ถ้าไม่มี `resident_name` จะแสดง `owner_name` แทน
- ถ้าไม่มีทั้งคู่ จะแสดง "ลูกบ้านห้อง [หมายเลขห้อง]"

## ไฟล์ที่แก้ไข
1. `app/portal/dashboard/page.tsx` - แก้ไขการแสดงชื่อ
2. `lib/supabase/ultra-simple-auth.ts` - เพิ่มการดึงข้อมูล resident_name
3. `app/portal/login/page.tsx` - แก้ไขข้อความต้อนรับ

## การทดสอบ
1. เข้าสู่ระบบด้วย username และ password
2. ตรวจสอบว่าชื่อที่แสดงเป็นชื่อจริงของลูกบ้าน
3. ถ้ายังแสดงชื่อ generic ให้ตรวจสอบข้อมูลในฐานข้อมูล

## หมายเหตุ
- ต้องอัปเดตชื่อจริงในฐานข้อมูลก่อน
- ระบบจะทำงานได้ทันทีหลังจากอัปเดตฐานข้อมูล
- สามารถเพิ่มชื่อใหม่ได้โดยอัปเดตตาราง `units` โดยตรง


















