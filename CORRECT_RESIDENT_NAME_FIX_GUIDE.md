# 🔧 แก้ไขปัญหาการแสดงชื่อลูกบ้าน - เวอร์ชันที่ถูกต้อง

## ปัญหา
ระบบแสดงข้อความ "ยินดีต้อนรับ, เจ้าของห้อง 1001" แทนที่จะแสดงชื่อจริงของผู้อาศัยที่ลงทะเบียนด้วย username นั้น

## สาเหตุที่ถูกต้อง
- ระบบใช้ `username` ในตาราง `units` สำหรับการเข้าสู่ระบบ
- แต่ `owner_name` เป็นชื่อเจ้าของห้อง ไม่ใช่ชื่อของผู้อาศัยที่ลงทะเบียนด้วย username นั้น
- ใน 1 ห้องอาจมีผู้อาศัยหลายคน แต่ละคนมี username ต่างกัน
- ไม่มีฟิลด์สำหรับเก็บชื่อจริงของผู้อาศัยแต่ละคนที่ลงทะเบียนด้วย username

## วิธีแก้ไขที่ถูกต้อง

### 1. รัน SQL Script ใน Supabase
```sql
-- รันไฟล์ CORRECT_RESIDENT_NAME_FIX.sql
```

### 2. อัปเดตชื่อจริงของผู้อาศัยตาม username
แก้ไขไฟล์ `CORRECT_RESIDENT_NAME_FIX.sql` ในส่วน:
```sql
WHEN username = 'ADD' THEN 'อานนท์ ดีใจ'
WHEN username = 'SM001' THEN 'สมศักดิ์ มั่นใจ'
WHEN username = 'T001' THEN 'ธนาคาร เงินดี'
-- เพิ่มตาม username ที่มีจริง
```

### 3. การทำงานของระบบ
- ระบบจะแสดง `resident_name` ก่อน (ชื่อจริงของผู้อาศัยที่ลงทะเบียนด้วย username นั้น)
- ถ้าไม่มี `resident_name` จะแสดง `owner_name` (ชื่อเจ้าของห้อง)
- ถ้าไม่มีทั้งคู่ จะแสดง "ผู้อาศัยห้อง [หมายเลขห้อง]"

## ความแตกต่างจากเวอร์ชันเดิม
- **เวอร์ชันเดิม**: พยายามแก้ไข `owner_name` ซึ่งเป็นชื่อเจ้าของห้อง
- **เวอร์ชันใหม่**: เพิ่ม `resident_name` สำหรับเก็บชื่อจริงของผู้อาศัยแต่ละคนที่ลงทะเบียนด้วย username

## ไฟล์ที่แก้ไข
1. `app/portal/dashboard/page.tsx` - แก้ไขการแสดงชื่อ
2. `lib/supabase/ultra-simple-auth.ts` - เพิ่มการดึงข้อมูล resident_name
3. `app/portal/login/page.tsx` - แก้ไขข้อความต้อนรับ

## การทดสอบ
1. เข้าสู่ระบบด้วย username และ password
2. ตรวจสอบว่าชื่อที่แสดงเป็นชื่อจริงของผู้อาศัยที่ลงทะเบียนด้วย username นั้น
3. ถ้ายังแสดงชื่อ generic ให้ตรวจสอบข้อมูลในฐานข้อมูล

## หมายเหตุสำคัญ
- ต้องอัปเดตชื่อจริงในฐานข้อมูลก่อน
- ชื่อจะถูกผูกกับ `username` ไม่ใช่ `unit_number`
- ใน 1 ห้องอาจมีผู้อาศัยหลายคน แต่ละคนมี username และชื่อต่างกัน
- ระบบจะทำงานได้ทันทีหลังจากอัปเดตฐานข้อมูล
















