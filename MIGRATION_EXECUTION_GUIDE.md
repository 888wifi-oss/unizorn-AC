# 🚀 **Migration Execution Guide**

## **📋 ขั้นตอนการรัน Migration**

### **1. เข้าสู่ Supabase Dashboard**
1. เปิดเว็บไซต์ [supabase.com](https://supabase.com)
2. เข้าสู่ระบบด้วยบัญชีของคุณ
3. เลือกโปรเจค condo-pro-dashboard

### **2. เปิด SQL Editor**
1. คลิกที่เมนู "SQL Editor" ในแถบด้านซ้าย
2. คลิกปุ่ม "New query" เพื่อสร้าง query ใหม่

### **3. รัน Migration Script**
1. คัดลอกเนื้อหาจากไฟล์ `scripts/141_complete_migration_execution.sql`
2. วางลงใน SQL Editor
3. คลิกปุ่ม "Run" เพื่อรัน script

### **4. ตรวจสอบผลลัพธ์**
- Script จะแสดงผลลัพธ์ทีละขั้นตอน
- ตรวจสอบว่าไม่มี error
- ดูผลลัพธ์การ verification ในขั้นตอนสุดท้าย

---

## **⚠️ สิ่งที่ต้องระวัง**

### **🔒 สำรองข้อมูลก่อน Migration**
```sql
-- สำรองข้อมูลก่อนรัน migration
-- (ทำใน SQL Editor แยกต่างหาก)
```

### **⏰ เวลาที่ใช้**
- Migration จะใช้เวลาประมาณ 2-5 นาที
- ขึ้นอยู่กับจำนวนข้อมูลที่มีอยู่

### **🔄 หากเกิด Error**
- ตรวจสอบ log ใน Supabase
- ดูข้อความ error ที่แสดง
- ติดต่อทีมพัฒนาเพื่อขอความช่วยเหลือ

---

## **✅ ตรวจสอบหลัง Migration**

### **1. ตรวจสอบตารางใหม่**
```sql
-- ตรวจสอบตารางที่สร้างใหม่
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
  AND table_name IN ('owners', 'tenants', 'tenancy_history', 'rental_payments');
```

### **2. ตรวจสอบข้อมูลที่ Migrate**
```sql
-- ตรวจสอบจำนวนข้อมูล
SELECT 
  'Units' as table_name, COUNT(*) as count FROM units
UNION ALL
SELECT 
  'Owners' as table_name, COUNT(*) as count FROM owners
UNION ALL
SELECT 
  'Tenants' as table_name, COUNT(*) as count FROM tenants;
```

### **3. ตรวจสอบ Foreign Keys**
```sql
-- ตรวจสอบ foreign key constraints
SELECT 
  conname,
  pg_get_constraintdef(oid)
FROM pg_constraint
WHERE conrelid IN (
  'public.units'::regclass,
  'public.owners'::regclass,
  'public.tenants'::regclass
);
```

---

## **🎯 หลัง Migration เสร็จ**

### **1. ทดสอบระบบ**
- เข้าหน้า Units ในแอปพลิเคชัน
- ตรวจสอบว่าข้อมูลแสดงถูกต้อง
- ทดสอบการเพิ่มห้องชุดใหม่
- ทดสอบการเพิ่มเจ้าของและผู้เช่า

### **2. ตรวจสอบฟีเจอร์ใหม่**
- ✅ การจัดการห้องชุดครบถ้วน
- ✅ การจัดการเจ้าของ
- ✅ การจัดการผู้เช่า
- ✅ ประวัติการเช่า
- ✅ การชำระค่าเช่า

### **3. อัปเดตทีม**
- แจ้งทีมว่าการ Migration เสร็จแล้ว
- แจ้งฟีเจอร์ใหม่ที่สามารถใช้งานได้
- แจ้งการเปลี่ยนแปลงที่สำคัญ

---

## **📞 การขอความช่วยเหลือ**

หากพบปัญหาหรือต้องการความช่วยเหลือ:
1. ตรวจสอบ log ใน Supabase
2. ดูข้อความ error ที่แสดง
3. ติดต่อทีมพัฒนา
4. แจ้งรายละเอียดปัญหาและ error message

---

## **🎉 สรุป**

**Migration Script พร้อมรันแล้ว!** 

คุณสามารถ:
1. **รัน Script** ใน Supabase SQL Editor
2. **ตรวจสอบผลลัพธ์** ตามขั้นตอน
3. **ทดสอบระบบ** หลัง Migration เสร็จ
4. **ใช้งานฟีเจอร์ใหม่** ได้ทันที

**พร้อมเริ่ม Migration!** 🚀
