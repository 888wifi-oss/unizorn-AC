# ✅ Checklist การตั้งค่าระบบ Invitation

## 📋 สรุปปัญหาและวิธีแก้ไข

### ปัญหาที่ 1: RLS Policy Error
**Error:** `new row violates row-level security policy for table "invitations"`

**วิธีแก้ไข:**
```sql
-- รันไฟล์: scripts/149_fix_invitations_rls.sql
```

### ปัญหาที่ 2: Invalid API Key
**Error:** `Authentication error: Invalid API key`

**วิธีแก้ไข:**
1. ไปที่ https://app.supabase.com/project/anhxilzznwtbnkijyidd/settings/api
2. คัดลอก service_role key
3. แก้ไขไฟล์ `.env.local`:
   ```
   SUPABASE_SERVICE_ROLE_KEY=<paste-your-key-here>
   ```
4. รีสตาร์ท dev server

### ปัญหาที่ 3: Missing user_id Column
**Error:** `Could not find the 'user_id' column of 'units' in the schema cache`

**วิธีแก้ไข:**
```sql
-- รันไฟล์: scripts/150_add_user_id_to_units.sql
```

## 🚀 ขั้นตอนการติดตั้งทั้งหมด

### 1. รัน SQL Scripts ใน Supabase SQL Editor

```sql
-- Script 1: สร้างตาราง invitations (พร้อม RLS ที่ถูกต้อง)
-- รันไฟล์: scripts/148_create_invitations_table.sql

-- Script 2: เพิ่ม user_id column ในตาราง units
-- รันไฟล์: scripts/150_add_user_id_to_units.sql
```

### 2. ตั้งค่า Environment Variables

แก้ไขไฟล์ `.env.local`:

```env
NEXT_PUBLIC_SUPABASE_URL="https://anhxilzznwtbnkijyidd.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
SUPABASE_SERVICE_ROLE_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..." # ← แก้ไขบรรทัดนี้!
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
```

### 3. รีสตาร์ท Dev Server

```bash
# หยุด dev server (Ctrl+C)
npm run dev
```

### 4. ทดสอบระบบ

1. **สร้างรหัสเชิญ:**
   - ไปที่หน้า "จัดการบัญชีผู้ใช้ Portal"
   - กดปุ่ม "ส่งรหัสเชิญ"
   - กรอกอีเมล (optional)
   - กด "สร้างรหัสเชิญ"

2. **สร้างบัญชีด้วยรหัสเชิญ:**
   - ไปที่ `http://localhost:3000/portal/register?code=<your-code>`
   - กรอก username, email, password
   - กด "สร้างบัญชี"

3. **ทดสอบเข้าสู่ระบบ:**
   - ไปที่หน้า login
   - เข้าสู่ระบบด้วย username และ password ที่สร้างไว้

## ✅ Checklist การตรวจสอบ

- [ ] รัน SQL scripts ทั้งหมดแล้ว
- [ ] แก้ไข SUPABASE_SERVICE_ROLE_KEY แล้ว
- [ ] รีสตาร์ท dev server แล้ว
- [ ] สร้างรหัสเชิญได้
- [ ] สร้างบัญชีด้วยรหัสเชิญได้
- [ ] เข้าสู่ระบบได้

## 🐛 Troubleshooting

### ถ้ายังมี error "Failed to fetch"
1. ตรวจสอบ console log ใน DevTools (F12)
2. ดู error message ที่ชัดเจน
3. แนบ console log มาให้ช่วยแก้ไข

### ถ้ามี error "Invalid API key"
1. ตรวจสอบว่าคัดลอก service_role key ถูกต้อง
2. ไม่มีช่องว่างหรือตัวอักษรพิเศษ
3. รีสตาร์ท dev server อีกครั้ง

### ถ้ามี error "Could not find user_id column"
1. รันไฟล์ `scripts/150_add_user_id_to_units.sql` อีกครั้ง
2. ตรวจสอบใน Supabase Table Editor ว่ามีคอลัมน์ user_id แล้ว

## 📞 ขอความช่วยเหลือ

ถ้ายังมีปัญหา:
1. แนบ error message จาก console (F12)
2. บอกว่าเกิด error ตอนขั้นตอนไหน
3. แนบ screenshot (ถ้ามี)

